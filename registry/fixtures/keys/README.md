# Fixture signing keys

These keys exist only to make the private-alpha and automated-test metadata
reproducible. Their private values are deliberately committed and provide no
trust outside those fixtures.

Never use these keys for a public catalog. Public releases require a separate
key ceremony, offline root and targets keys, and protected online snapshot and
timestamp keys.
